package uz.targetsoftwaredevelopment.carcounter.utils

/*

  "Cars",
        "Minibuses",
        "Buses",
        "Trucks up to 3.5 t",
        "Trucks up to 3.5–12 t",
        "Trucks above 12 t",
        "Road trains",
        "Other"

fun saveData(vehicleType: String, direction: String, count: Int) {
    val timestamp = LocalDateTime.now()
    val date = timestamp.toLocalDate()
    val time = timestamp.format(DateTimeFormatter.ofPattern("HH:mm"))

    val file = File("traffic_data.csv")
    val writer = PrintWriter(BufferedWriter(FileWriter(file, true)))
    writer.println("$vehicleType,$direction,$count,$date,$time")
    writer.close()
}
*/
